<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="second_team_config_action" model="ir.actions.act_window">
        <field name="name">Second Sales Teams</field>
        <field name="res_model">second.sales.team</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="view_id" ref="sales_team.crm_team_view_search"/>
    </record>


    <menuitem id="second_team_config"
            name="Second Sales Teams"
            parent="crm.crm_menu_config"
            action="second_team_config_action"
            sequence="6"/>
</odoo>